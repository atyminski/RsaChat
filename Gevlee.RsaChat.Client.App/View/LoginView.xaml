<Window x:Class="Gevlee.RsaChat.Client.App.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gevlee.RsaChat.Client.App.View"
        mc:Ignorable="d"
        Title="Logowanie" Height="134.892" Width="420.863">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="113*" />
			<ColumnDefinition Width="93*" />
			<ColumnDefinition Width="207*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="91*" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
		<TextBlock Text="Łączenie..." IsEnabled="{Binding ConnectionPending}" Grid.Row="1" Grid.Column="1"
		           HorizontalAlignment="Center" VerticalAlignment="Center" />
		<TextBlock Text="Nick:" IsEnabled="{Binding ConnectionPending}" Grid.Row="0" Grid.Column="0"
		           HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" />
		<TextBox Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="1" VerticalAlignment="Center" />
		<Button Content="Zaloguj" Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center"
		        Padding="5" />
	</Grid>
</Window>